[project]
name = "simple"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cadquery>=2.5.2",
    "pydantic>=2.11.7",
]

[tool.ruff.lint.isort]
known-first-party = ["click_cadquery"]
